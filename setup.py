from setuptools import setup, find_packages

setup(
    name='webrtc_stats',
    version='1.0.0',
    description='A Python library for webrtc internals dump file analyzing',
    author='Walter Fan',
    author_email='walter.fan@gmail.com',
    packages=find_packages(),
    install_requires=[],
)
